{
  "name": "worker-inspector",
  "version": "1.0.0",
  "description": "Program that inspects if worker processed are running longer than we would like.",
  "main": "dist/index.js",
  "dependencies": {
    "axios": "^0.19.0",
    "typescript": "^3.6.3"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc && node dist/index.js"
  },
  "author": "Luka Reisman",
  "license": "ISC"
}
